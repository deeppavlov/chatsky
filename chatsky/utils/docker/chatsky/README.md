# Chatsky Docker Image

## Description

To run this image, navigate to the directory where `compose.yml` is located. Create a directory and add its name in volumes in `compose.yml` like
```
volumes:
    - ./<MY_PROJECT>/:/app/
```
