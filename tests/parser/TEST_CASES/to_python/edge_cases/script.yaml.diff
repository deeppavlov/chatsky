  main:
    Actor: from dff.core.engine.core.actor import Actor
    TRANSITIONS: from dff.core.engine.core.keywords import TRANSITIONS
    transitions: from .transitions import transitions
    variables: import variables
    script:
      "'start_flow'":
        "'start_node'":
          TRANSITIONS:
            transitions['two']: 'len(transitions[1]) > 0 > -2 > (lambda : -5)() > func()'
        "'label'":
-         TRANSITIONS: transitions[7]
?                                  ^
+         TRANSITIONS: transitions[8]
?                                  ^
-       "'another'":
?         - --- ^
+       "'new'":
?           ^
          TRANSITIONS: transitions[variables.number]
    actor: Actor(script, start_label=('start_flow', 'start_node'), some_kwarg=another_func)
  transitions:
    number: from .variables import number
    label: from .variables import label
    condition: from .variables import condition
    transitions:
      '1': "'cnd'"
      "'two'": "'label'"
      number:
        label: condition
      "'set'": '{1, 2}'
  variables:
+   number: '8'
+   condition: 'True'
    label: ('start_flow', 'start_node')
-   condition: 'True'
-   number: '7'
